---
- name: delete application
  hosts: all
  tasks:
          - name: remove app
            ansible.builtin.file:
                    path: ~/matthew-project0-production
                    force: yes
                    state: absent
          - name: remove data
            ansible.builtin.file:
                    path: ~/project-data
                    force: yes
                    state: absent
